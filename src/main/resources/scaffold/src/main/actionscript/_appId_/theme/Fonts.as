package _appId_.theme
{
	/**
	 * Embed your fonts here.
	 * Using fonts/generateFontClass gradle task will automatically generate this file from your src/main/fonts directory.
	 * By using font gradle task you need to add "Bold/Italic/Cff" explicitly in your font name (for example ArialBold.ttf, ArialBoldItalicCff, VerdanaCff),
	 * for better readability and class property names generation use CamelCase.
	 */
	public final class Fonts
	{
		/**
		 * @private
		 */
		public function Fonts()
		{
			throw new Error( this + " cannot be instantiated" );
		}
	}
}
