package _appId_.theme
{
	/**
	 * Embed your fonts here.
	 * Using fonts/generateFontClass gradle task will automatically generate this file from your src/main/fonts directory.
	 * By using font gradle task you need to add "Bold/Italic/Cff" explicitly in your font file name (for example arialBold.ttf, arialBoldItalicCff.ttf, verdanaCff.ttf),
	 * for better readability and class property name generation use camelCase with your font file names.
	 */
	public final class Fonts
	{
		/**
		 * @private
		 */
		public function Fonts()
		{
			throw new Error( this + " cannot be instantiated" );
		}
	}
}
